# Cattleya Website

## Run Project

1. Build Docker containers:
   ```
   docker-compose up -d --build
   ```

2. Generate `.env`:
   ```
   php generate_env.php
   ```

3. Open browser: [http://localhost:8000](http://localhost:8000)